{
  "name": "openai-node-stream",
  "version": "1.0.0",
  "description": "Streaming example with openai-node",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf ./dist && tsc --downlevelIteration --skipLibCheck --module commonjs --target es6 --outDir dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fortaine/openai": "^4.0.1",
    "uuid": "^8.3.2",
    "koa": "^2.7.0",
    "koa-body": "^4.1.0",
    "koa-router": "^8.0.0-alpha.1"
  }
}
